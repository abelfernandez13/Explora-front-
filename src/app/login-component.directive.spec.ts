import { LoginComponentDirective } from './login-component.directive';

describe('LoginComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginComponentDirective();
    expect(directive).toBeTruthy();
  });
});
